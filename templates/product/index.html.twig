{% extends 'base.html.twig' %}

{% block body %}
    <div>

        <div class="col-12 col-lg-7">
            <img src="/img/{{ product.image }}" alt="{{ product.designation }}">
        </div>

        <div class="col-12 col-lg-5 p-t-15 p-b-15">
            <h2>Les promos de la semaine</h2>
            <hr class="H2_underline">
        </div>
        {{ product.designation }}
        {{ product.price }} €
        {{ product.description }}
        {{ product.content|raw }}

        {% if product.stock == 0 %}
            <p>Out of stock :(</p>
        {% else %}
            <form method="post" action="{{ path('cart_add', {'id' : product.id, 'quantite': 1}) }}">
                <input type="number" value="1" id="how" name="how" min="1" max="30">
                <button type="submit">Ajouter au panier</button>
            </form>
        {% endif %}

        <div class="p-t-15 p-b-15">
            <h2>Les promos de la semaine</h2>
            <hr class="H2_underline">
        </div>

        <div class="d-flex flex-wrap justify-content-around justify-content-lg-between card-list">

            <!-- Pour les infos, voir dans la base de données les noms !-->
{#            {% for item in promoList %}#}
{#                <div class="col-12 col-lg-4 card">#}
{#                    <img class="p-x-15 p-y-15" src="img/{{ item.product.image }}" alt="">#}
{#                    <div class="d-flex flex-wrap promo-list">#}
{#                        <div class="col-12">#}
{#                            <a class="title" href="{{ path('product_show', {'slug': item.product.slug}) }}">{{ item.product.designation }}</a>#}
{#                        </div>#}
{#                        <div class="col-10">#}
{#                            <p class="subtitle">{{ item.product.category.title }}</p>#}
{#                            <p>{{ item.product.description|length > 40 ? item.product.description|slice(0, 40) ~ '...' : item.product.description  }}</p>#}
{#                        </div>#}
{#                        <div class="col-2 promo-list_price">#}
{#                            <p>{{ item.product.price }} <span>€</span></p>#}
{#                        </div>#}
{#                    </div>#}
{#                    <button class="btn btn-guys">Ajouter au panier</button>#}
{#                </div>#}
{#            {% endfor %}#}
        </div>

    </section>

{% endblock %}
